Never write placeholder code.

Never skip validation.

Always use TypeScript.

Every component must be reusable.

Every API must include tests.

Every feature must be documented.

Never break existing functionality.

Every UI must be responsive.

Always use Tailwind.

Always animate interactions.

Always use React hooks.

Use Phaser.js for mini-games.

Use MongoDB.

Use Fastify.

Every game must support localization.

Every screen must support accessibility.

Never hardcode text.

All strings come from localization files.
